import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.minalouisleon.mydesktopapp',
  appName: 'my-desktop-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
